<%= simple_form_for(@interest) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :icon %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
